fastapi[standard]
uvicorn[standard]
jinja2
Babel
python-multipart
itsdangerous

# Dependências de teste
pytest
pytest-asyncio
pytest-cov

DE ubuntu:22.04
EXECUTE apt-get update
EXECUTE apt-get install -y python3 python3-pip git

WORKDIR /aplicativo

EXECUTE git clone https://github.com/RebekaSerafim/CaseBem.git .
EXECUTE pip3 install --no-cache-dir -r requirements.txt

CMD ["uvicorn", "principal:aplicativo", "--host", "0.0.0.0", "--porta", "80"]